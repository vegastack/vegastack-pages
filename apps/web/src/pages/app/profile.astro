---
// /app/profile redirected into the settings IA in 0.2.0. Keep the route alive
// so existing bookmarks / outbound links resolve.
export const prerender = false;
return Astro.redirect("/app/settings/profile", 301);
---
